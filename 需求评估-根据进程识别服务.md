# 需求评估：根据进程识别端口服务

## 进程识别端口服务

问题：`当前系统对占用端口只能显示"未知应用"。扫描结果中仅 ports.json 已配置的 38082 端口能正确显示名称（Docker Port Hub），其余 7 个真实占用的端口（38080/38081/38083/38090/38091/38101/38400）全部显示为"未知应用"。这是由 perform_scan() 的合并逻辑决定的：name = ports.json 配置 > docker 容器名 > "未知应用"。由于部署环境中 docker 命令不可用，docker_detect 返回空，且 ports.json 只覆盖了少量端口，导致大量端口无法识别。`

`具体代码位于 backend/app.py:118-121 的合并逻辑：`
```python
item = {
    'name': app_config.get('name') or docker_info.get('container_name') or '未知应用',
    ...
}
```

`修改方案：新增一个 ProcessDetect 模块，通过 netstat -tlnp 获取各端口的进程 PID，然后从 /proc/<pid>/cmdline 和 /proc/<pid>/cwd 提取完整命令行和工作目录，解析出可读的服务名称。然后在 perform_scan() 的合并逻辑中增加"进程识别"作为第二优先级（介于 ports.json 配置和"未知应用"之间）。`

`具体修改：`

`1. 新建 backend/process_detect.py — 进程检测模块`
   - 调用 `netstat -tlnp` 解析出 `{port: {pid, program, cmdline, cwd}}`
   - 从 `/proc/<pid>/cmdline` 提取完整命令
   - 从 `/proc/<pid>/cwd` 提取工作目录（符号链接目标）
   - 提供 `get_process_info(port)` 和 `detect_all()` 两个方法
   - 服务名称生成规则（按优先级）：
     a. 从 cmdline 中提取脚本名/项目名（如 `python main.py` → `main`）
     b. 从 cwd 目录名提取（如 `/data/proVideoDownLoadTools` → `proVideoDownLoadTools`）
     c. 从程序名提取（如 `python` → `python`）
   - 注意：netstat 可能需要 root 权限来显示 PID（当前环境可行），如果不可读则返回空

`2. 修改 backend/app.py — 集成 ProcessDetect`
   - 在 `perform_scan()` 中新增 `process_detect.detect_all()` 调用
   - 合并逻辑改为：`name = app_config.name or process_info.service_name or docker_info.container_name or "未知应用"`
   - 新增 `process` 字段（可选），返回进程名信息

`3. 修改 backend/process_detect.py（可选）— 增加智能识别规则`
   - 对常见应用做映射：vue-cli-service → Vue 前端开发服务器, vite → Vite 开发服务器, tsx → TypeScript 应用, main.py → 根据目录名命名
   - 对 node 项目尝试读取 package.json 中的 name 字段

`可行性：可行`
`难度：低`

`验证数据：`
```
当前环境通过 netstat + /proc 可识别到：
  38081  python main.py           /data/proVideoDownLoadTools  → "视频下载工具"
  38083  python app.py --port 38083  /data/yudao_launcher      → "后台管理"
  38090  tsx src/index.ts          /data/tmuxUI/tmux-web/server → "tmux-web 终端管理"
  38091  vite --host 0.0.0.0       /data/tmuxUI/tmux-web/web    → "tmux-web 前端"
  38101  vue-cli-service serve     /data/svn_code/rh-file_ui    → "文件管理"
  38400  node sync-server.cjs      /data/server                 → "同步服务"
```