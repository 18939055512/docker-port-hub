# 需求评估：端口占用判断逻辑

## TCP connect_ex 检测在 docker-proxy 环境下产生假阳性

问题：`当前端口占用判断完全依赖 socket.connect_ex() 返回 0 即判定为 RUNNING。但用户服务器上存在容器 ubuntu_tede-ubuntu_tEDe-1，映射了 0.0.0.0:38080-38579->38080-38579/tcp，docker-proxy 在宿主机上对所有 500 个端口完成 TCP 握手（connect_ex == 0）。当扫描目标为宿主机 IP 时，即使容器内端口无应用监听，所有端口都被标记为 RUNNING（假阳性）。用户实际验证：curl 38092 显示"连接被对方重置"（RST），但 TCP 连接建立阶段是成功的。`

修改方案：
```
backend/scanner.py line 28-35 — 修改 scan_port 方法，增加连接后的探测验证：

def scan_port(self, port):
    try:
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        sock.settimeout(self.CONNECT_TIMEOUT)
        result = sock.connect_ex((self.host, port))
        if result != 0:
            sock.close()
            return port, False
        # 二次验证：发送探测数据，检查是否被 RST
        sock.settimeout(1)
        try:
            # 发送空 HTTP 请求或简单探测字节
            sock.sendall(b'GET / HTTP/1.0\r\nHost: localhost\r\n\r\n')
            # 尝试接收，如果收到 RST，recv 会抛出异常
            sock.recv(1)
            sock.close()
            return port, True
        except (socket.error, ConnectionResetError, BrokenPipeError, OSError):
            sock.close()
            return port, False
    except Exception:
        return port, False
```

注意：此方案对非 HTTP 服务（如 MySQL 3306、Redis 6379）可能误判为 EMPTY，因为发送 HTTP 请求后可能被立即断开。可改为发送 1 字节探测数据 + 短超时收包，任何响应（包括 RST 之外的异常）都视为 RUNNING。

可行性：可行
难度：低

## Docker port 范围格式解析不完整

问题：`docker_detect.py 第 67 行的正则 (\d+)-> 只匹配端口范围中的第一个数字。当 docker ps 的 Ports 字段为 "0.0.0.0:38080-38579->38080-38579/tcp" 时，只解析出 38080 一个端口，未能展开 38080-38579 的完整范围，导致 Docker 容器信息与端口无法正确关联。`

修改方案：
```
backend/docker_detect.py line 65-74 — 重构 _parse_container 方法，增加范围展开处理：

def _parse_container(self, container):
    try:
        name = container.get('Names', '').replace('/', '')
        image = container.get('Image', '')
        state = container.get('State', '')
        ports_str = container.get('Ports', '')
        if not ports_str:
            return
        for mapping in ports_str.split(','):
            mapping = mapping.strip()
            # 匹配范围格式: 0.0.0.0:38080-38579->38080-38579/tcp
            range_match = re.search(r'(\d+)-(\d+)->(\d+)-(\d+)', mapping)
            if range_match:
                start_host = int(range_match.group(1))
                end_host = int(range_match.group(2))
                for port in range(start_host, end_host + 1):
                    self._container_cache[port] = {
                        'container_name': name, 'image': image, 'status': state
                    }
                continue
            # 匹配单个端口: 0.0.0.0:38080->8080/tcp
            single_match = re.search(r':(\d+)->', mapping)
            if single_match:
                host_port = int(single_match.group(1))
                self._container_cache[host_port] = {
                    'container_name': name, 'image': image, 'status': state
                }
    except Exception as e:
        print(f"[Docker] 解析容器信息失败: {e}")
```

可行性：可行
难度：低

## 扫描主机解析路径存在歧义

问题：`resolve_scan_host() 从 access.prefix 提取 IP 作为扫描目标（app.py 第 76-78 行）。当 Dashboard 在宿主机上运行时，扫描 192.0.2.10 会经过 docker-proxy 导致假阳性；当 Dashboard 在容器内运行时，扫描 127.0.0.1 又只能看到容器自身端口，无法检测宿主机映射。当前示例配置 access.prefix 为 http://192.0.2.10，port.host 为 127.0.0.1，触发自动解析为 192.0.2.10，恰好是产生假阳性的路径。`

修改方案：
```
backend/app.py — 在 resolve_scan_host 之后增加部署模式判断逻辑：

建议两步：
1. 新增配置项 scan.deploy_mode: host | container，用户明确指定运行模式
2. host 模式：扫描 127.0.0.1（宿主机本地不会经过 docker-proxy）
3. container 模式：扫描宿主机 IP（从 access.prefix 提取）

或更简单：
1. 删除 resolve_scan_host 自动解析逻辑
2. 明确要求用户在 port.host 中填写正确的扫描目标
3. 在 application.yml 中给注释说明

当前 host: 127.0.0.1 被自动覆盖为 192.0.2.10 的行为是隐蔽的，应改为明确配置。
```

可行性：需进一步评估（取决于用户接受显式配置还是自动检测）
难度：低

## Dashboard 自身端口在扫描结果中

问题：`Dashboard 运行在 38082，在扫描范围 38080-38579 内。TCP 扫描会检测到自身端口开放，导致端口列表中显示 "38082 未知应用 RUNNING"。虽然已在 ports.json 中标记为 "Docker Port Hub"，但扫描自身端口存在轻微性能浪费。`

修改方案：
```
backend/scanner.py line 42 — 在 scan_all 方法中跳过服务器自身端口：

在 scan_all 中添加参数 exclude_ports 或从 app.py 传入 SERVER_PORT：
    self.self_port = SERVER_PORT  # 构造函数传入
    def scan_all(self):
        ports = [p for p in range(self.start_port, self.end_port + 1) if p != self.self_port]
```

或直接在 app.py 的 perform_scan 中过滤：
```
backend/app.py line 113 — 在循环中跳过 SERVER_PORT：
    if port == SERVER_PORT:
        continue
```

可行性：可行
难度：低
